PORT=3000
JWT_SECRET=change-me-to-a-long-random-string
DB_PATH=./data/cipher.db
CORS_ORIGIN=http://localhost:3000
# Optional: Discord/Slack webhook that receives every new join application
JOIN_WEBHOOK_URL=
# Optional: where uploaded photos are stored (defaults to <db folder>/uploads)
UPLOAD_DIR=
